if (false || true) {
  console.log('Yes!');
} else {
  console.log('No...');
}

// The else block will never be reached. It will always log 'Yes!'
// The expression provided as an argument to the if statement will always evaluate to true.
